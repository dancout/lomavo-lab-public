# Homepage environment variables
# Copy to .env and fill in actual values
#
# How templating works:
# - Variables prefixed with HOMEPAGE_VAR_ are available in config files (services.yaml, etc.)
# - Reference them with {{HOMEPAGE_VAR_NAME}} syntax in YAML files
# - The docker-compose.yml uses `env_file: .env` to pass these to the container
# - This keeps secrets AND network-specific values out of version-controlled config files

# =============================================================================
# Network IPs (used in services.yaml for widget URLs)
# =============================================================================
HOMEPAGE_VAR_RPI_IP=
HOMEPAGE_VAR_GAMING_PC_IP=
HOMEPAGE_VAR_NAS_IP=
HOMEPAGE_VAR_ROUTER_IP=

# =============================================================================
# Homepage allowed hosts (for docker-compose.yml)
# =============================================================================
HOMEPAGE_ALLOWED_HOSTS=home.yourdomain.com,localhost:3000

# =============================================================================
# API Keys
# =============================================================================
# Immich API key for stats widget (get from Immich > User Settings > API Keys)
# Requires: server.statistics permission
# Referenced in services.yaml as {{HOMEPAGE_VAR_IMMICH_API_KEY}}
HOMEPAGE_VAR_IMMICH_API_KEY=

# Pi-hole app password for stats widget (shared by both primary and backup)
# Get from: Pi-hole UI > Settings > Web Interface/API > Expert > Configure app password
# Must be an app password, not the web login password (see ADR-024)
# Referenced in services.yaml as {{HOMEPAGE_VAR_PIHOLE_API_KEY}}
HOMEPAGE_VAR_PIHOLE_API_KEY=

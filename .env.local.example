# Local Configuration
# Copy this file to .env.local and fill in your actual values.
# .env.local is gitignored and contains your real IPs, usernames, and secrets.

# =============================================================================
# NETWORK - Machine IPs
# =============================================================================
RPI_IP=
GAMING_PC_IP=
NAS_IP=
MACBOOK_IP=             # Temporary: Ollama inference host (DHCP, may change)
ROUTER_IP=
VIP=                    # Keepalived Virtual IP for Pi-hole HA

# =============================================================================
# SSH - Usernames for each machine
# =============================================================================
RPI_USER=
GAMING_PC_USER=
NAS_USER=

# =============================================================================
# EXTERNAL URLS - Your public-facing domain/URLs
# =============================================================================
DOMAIN=
STATUS_URL=
PHOTOS_URL=

# =============================================================================
# HOMEPAGE - Dashboard configuration
# =============================================================================
# Comma-separated list of allowed hosts for Homepage
HOMEPAGE_ALLOWED_HOSTS=home.yourdomain.com,10.0.0.X:3000

# =============================================================================
# SERVICE TOKENS
# =============================================================================
UPTIME_KUMA_PUSH_TOKEN=     # Push token from Uptime Kuma (used by Watchtower notifications)

# =============================================================================
# GAMING PC PATHS - Used by MCP server docker-compose
# =============================================================================
REPO_PATH=                  # Path to lomavo-lab repo clone on Gaming PC
SSH_KEY_DIR=                # Path to .ssh directory on Gaming PC

# =============================================================================
# SERVICES - Ports (defaults shown, change if yours differ)
# =============================================================================
HOMEPAGE_PORT=3000
UPTIME_KUMA_PORT=3001
PIHOLE_PORT=80
IMMICH_PORT=2283
JELLYFIN_PORT=8096
GLANCES_PORT=61208
METRICS_ENDPOINT_PORT=61209
OLLAMA_PORT=11434
OPEN_WEBUI_PORT=3080
MCP_HOMELAB_PORT=8770
MCP_MONITORING_PORT=8771
NAS_WEBUI_PORT=8080

# =============================================================================
# EXAMPLE SSH COMMANDS (for reference, constructed from above)
# =============================================================================
# Pi:        ssh ${RPI_USER}@${RPI_IP}
# Gaming PC: ssh "${GAMING_PC_USER}"@${GAMING_PC_IP}
# NAS:       ssh ${NAS_USER}@${NAS_IP}

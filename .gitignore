# Local configuration with real IPs/usernames/secrets
.env
.env.local

# User-specific homelab profile (created from .example by setup script)
HOMELAB_PROFILE.md

# Environment files with sensitive data (per-machine)
.env.rpi
.env.gaming-pc
.env.nas
rpi/.env
gaming-pc/.env
nas/.env
# Keep templates tracked
!.env.example
!rpi/.env.example
!gaming-pc/.env.example
!nas/.env.example

# MCP configuration (contains real IPs)
.mcp.json

# Node.js (MCP servers)
mcp-servers/node_modules/
mcp-servers/dist/
mcp-servers/package-lock.json

# Temporary backup directories (contain sensitive data)
.backup-originals/

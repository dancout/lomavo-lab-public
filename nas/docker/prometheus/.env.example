# Prometheus config variables
# These are resolved by deploy.sh from the repo-root .env.local file.
# You do NOT need a .env file here — deploy.sh handles substitution.

RPI_IP=           # Raspberry Pi IP (from .env.local)
GAMING_PC_IP=     # Gaming PC IP (from .env.local)
NAS_IP=           # QNAP NAS IP (from .env.local)
QDRANT_API_KEY=   # Qdrant Bearer token (from .env.local)
